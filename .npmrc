# Force a classic (hoisted) node_modules layout.
# This avoids issues on some Windows setups where pnpm's default linker may not create
# top-level entries for unscoped packages (react/vite/typescript), breaking scripts.
node-linker=hoisted
